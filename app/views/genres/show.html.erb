<div class="row">
	<div class="col-xs-3">
		<%= render 'sidebar', genres: @genres %>
	</div>

	<div class="col-xs-9">
		<h3 class="title"><%= @genre.name %>一覧<span>（全<%= @items.count %>件）</span></h3>
		<%= render 'items/item_index' , items: @items %>
	</div>
</div>