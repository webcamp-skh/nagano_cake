<div class="row">
	<div class="col-xs-3">
		<%= render 'genres/sidebar', genres: @genres %>
	</div>

	<div class="col-xs-9">
		<h3 class="title">商品一覧<span>（全<%= @items.count %>件）</span></h3>
		<%= render 'item_index' , items: @items %>
	</div>
</div>

