




		<%= form_for([:admin, @genres]) do |f| %>

		<%= f.text_field :name, class: "col-xs-12" %>

		<%= f.radio_button :status, true %>

		<%= f.radio_button :status, false %>

		<%= f.submit "追加", class: "btn btn-primary" %>

		<% end %>

		<% @genres.each do |genre| %>

		<%= genres.name %>

		<%= genres.status %>

		<% end %>